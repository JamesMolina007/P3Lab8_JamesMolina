/*
#pragma once

template< typename T >
class Vector {
    public:
        Vector( T, T, T );
        T getX(){ return x; }
        T getY(){ return y; }
        T getZ(){ return z; }
        int size(){ return vector.max_size; }
        Vector operator+( Vector v2 );
        Vector operator*( Vector v2 );
    private:
        T x;
        T y;
        T z;
};

*/

#include "Vector.hpp"

template<class T>
Vector::Vector( T x, T y, T z ){

}